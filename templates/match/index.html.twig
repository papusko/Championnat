{% extends 'base.html.twig' %}

{% block title %}Match index{% endblock %}

{% block body %}
    <h1>Match index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ScoreEquipeDomicile</th>
                <th>ScoreEquipeExterieur</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for match in matches %}
            <tr>
                <td>{{ match.id }}</td>
                <td>{{ match.scoreEquipeDomicile }}</td>
                <td>{{ match.scoreEquipeExterieur }}</td>
                <td>
                    <a href="{{ path('match_show', {'id': match.id}) }}">show</a>
                    <a href="{{ path('match_edit', {'id': match.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('match_new') }}">Create new</a>
{% endblock %}
